### 断言

```
#include <assert.h> 
assert(表达式);
```

- 如果断言的条件成立（为真），程序会继续正常执行，就像什么都没发生一样。
- 如果断言的条件不成立（为假），程序会立即终止，并打印出一条标准的错误信息，告诉你：
	- 哪个断言失败了。
	- 发生在哪个源文件。
	- 发生在哪一行代码。

# Lambda表达式

Lambda表达式是C++11引入的一种匿名函数对象，它允许你在代码中直接定义一个函数。Lambda表达式通常用于简单的操作，比如排序、遍历等。例如：

```c++
auto add = [](int a, int b) { return a + b; };
int result = add(3, 4);  // result = 7
```

在这个例子中，  add  是一个Lambda表达式，它定义了一个简单的加法操作。

==`[]` 是 Lambda 表达式的**捕获列表**，用来指定如何从外部作用域捕获变量。==

###### 捕获列表的几种写法

**`[&]` - 引用捕获**

```
int x = 10, y = 20;
auto add = [&](int a, int b) { 
    // 可以访问外部变量 x, y，且是引用方式（可修改原值）
    cout << x + y;  // 引用外部 x 和 y
    x = 100;        // 会修改外部的 x
    return a + b; 
};
```

**`[=]` - 值捕获**

```
int x = 10, y = 20;
auto add = [=](int a, int b) { 
    // 可以访问外部变量 x, y，但是拷贝（不可修改原值）
    cout << x + y;  // 使用外部 x 和 y 的拷贝
    // x = 100;     // 错误：不能修改，因为是拷贝
    return a + b; 
};
```

**混合捕获**

```
int x = 10, y = 20, z = 30;
auto func = [&x, y, &z](int a) {
    // x 和 z 是引用捕获，y 是值捕获
    x = 100;  // 可以，修改外部的 x
    // y = 200; // 错误，y 是值捕获
    z = 300;  // 可以，修改外部的 z
};
```

**不捕获（空 `[]`）**

```
auto add = [](int a, int b) { 
    // 不能访问任何外部变量
    return a + b; 
};
```

#### 迭代器作用

```c++
for (auto [v, length] : graph[u])
// 遍历u的所有邻接节点v和边权length
//graph是一个二维数组，数组中的每一个元素都是一个二元组
```

没有const和引用 - 会发生字符串拷贝（性能差）

```
for (auto token : postfix) {
   // 每次循环都会拷贝一个string
}
```

有const引用 - 不会拷贝（性能好）

```
for (const auto& token : postfix) {
    // 只是引用原字符串，零拷贝
}
```

#### 递归用法

在C++17中，引入了一种特殊的语法来实现递归Lambda表达式：  **this auto&&**  。

这种语法允许Lambda表达式在定义时引用自身。

语法解释：

•   this  ：表示当前对象的指针。

•   auto&&  ：表示一个右值引用，可以绑定到任何类型的对象。

•   this auto&&  ：允许Lambda表达式引用自身。

```c++
std::function<int(int)> factorial = [&](int n) -> int {
        if (n <= 1) return 1;  // 递归边界
        return n * factorial(n - 1);  // 递归调用
    };
```

-> int：表明返回的数据类型是int类型

```c++
//力扣题目 198.打家劫舍
auto dfs = [&](this auto&& dfs, int i) -> int {
            if (i < 0) { // 递归边界（没有房子）
                return 0;
            }
            if (memo[i] != -1) { // 之前计算过
                return memo[i];
            }
            return memo[i] = max(dfs(i - 1), dfs(i - 2) + nums[i]);
        };
```

## **Lambda表达式捕获列表（Capture List）**

捕获列表是Lambda表达式的一部分，用于指定Lambda可以访问哪些外部变量。捕获列表的语法是  [...]  ，其中可以包含以下几种形式：

#### 1.1   [&]  （按引用捕获所有变量）

• 作用：Lambda可以访问并修改所有外部变量。

• 比喻：给Lambda一副“全视角眼镜”，它能看到并修改所有外部变量。

• 适用场景：当你需要在Lambda中修改外部变量时，使用  [&]  。

#### 1.2   [=]  （按值捕获所有变量）

• 作用：Lambda可以访问所有外部变量的副本，但不能修改外部变量。

• 比喻：给Lambda一副“只读眼镜”，它能看到外部变量，但不能修改它们。

• 适用场景：当你只需要读取外部变量的值，而不希望修改它们时，使用  [=]  。

#### 1.3   [memo, &nums]  （混合捕获方式）

• 作用：可以按值捕获某些变量，同时按引用捕获其他变量。

•   **memo**  ：按值捕获，Lambda中使用  memo  的副本。

•   **&nums**  ：按引用捕获，Lambda可以直接访问和修改  nums  。

# 范围算法

**ranges算法**：    C++20引入

```c++
int neg = ranges::lower_bound(nums, 0) - nums.begin();
```

**注意：**这个二分查找的范围是  ==**[  **begin, end  **)**==，意思是 [ 0，i），如果你写了`lower_bound(nums.begin(),nums.begin()+i-1，0)`的话，那实际范围就是 [ 0，i-1 ），即 [0，i-2 ]

- `ranges::lower_bound(nums, 0)` 返回指向**第一个不小于0**的元素的迭代器                 ==“[”==
- `ranges::upper_bound(nums, 0)` 返回指向**第一个大于0**的元素的迭代器                        ==”）“==
- 在有序数组中，这实际上是第一个**非负数**（0或正数）的位置
- `- nums.begin()` 计算从开始到这个位置的元素个数，即所有**负数**的数量

**示例**：`nums = [-3,-2,-1,0,1,2,3]`

- `lower_bound(nums, 0)` 指向第一个0
- 索引差为3，即负数个数为3

==具体位置：==

```c++
lower_bound(nums.begin()+1,nums.end()-1,0);
```

